# 개인 홈페이지 (eunchanjoe.vercel.app / pi0203.github.io)

조은찬의 개인 사이트. Next.js 정적 내보내기 → GitHub Pages와 Vercel 양쪽에 자동 배포.

**무엇을 위한 자리인가 — 읽고 만드는 과정의 공개 작업 노트.** 알아보고, 그 위에 무언가를
만들고, 그 과정을 적어두는 곳이다. **읽은 것과 만든 것이 따로 노는 게 아니라 한 일의
앞뒤다.** 음식중독 문헌을 먼저 읽고 그 결론에 맞춰 도구의 모양을 바꾼 것이 그 본이다.

그래서 **어느 섹션이 두껍다고 줄이지 않는다.** 2026-09-11에 읽은 것이 64%였던 것은 관심이
쏠려서가 아니라 인스타 보관함을 발굴해 옮겨 적었기 때문이고, 만든 것이 4%였던 것은 아직
발굴하지 않았기 때문이다. **답은 줄이는 게 아니라 안 옮긴 쪽을 옮기는 것이다.**

## 구조

| 경로 | 내용 |
|---|---|
| `content/me/` | "나" — 형식 없는 항목이 시간순으로 쌓임 |
| `content/work/` | "만든 것" |
| `content/writing/` | "글" |
| `content/reading/` | "읽은 것" — 책과 그에 대한 생각 |
| `content/watching/` | "본 것" — 영화·드라마 |
| `app/` | 화면. 섹션을 추가할 때 말고는 손댈 일이 없다 |
| `lib/content.ts` | 마크다운을 읽어 페이지로 넘기는 유일한 통로 |

항목을 추가하는 방법은 **두 가지**다. 둘 다 코드를 고치지 않는다.

1. `content/<섹션>/`에 `.md` 파일을 하나 넣고 푸시한다
2. **`npm run studio`** — 입력 화면에서 쓰고 저장하면 파일이 만들어지고,
   확인 화면에서 버튼을 누르면 올라간다 (아래 명령어 절 참조)

파일 앞머리(front matter)는 `title`, `date`만 필수.
나머지(`tag`, `summary`, `author`, `link`, `then`, `about`)는 선택.

**`about` — 글이 다루는 시기.** `date`는 **글을 쓴 날**이고 `about`은 **언제의 나에 대한
글인가**다. 2021년 이야기를 2026년에 적을 수 있으므로 둘은 다르다. 지금은 「나」에서만 뜬다.

```yaml
date: 2026-09-09        # 쓴 날
about: "2020–2022"      # 다루는 시기 → 「2020–2022년에 대해」
```

- 연도 하나든 범위든 자유. **범위는 반드시 따옴표로 감싼다** — YAML이 `2021-2022`를
  날짜로 넘겨다볼 여지를 없앤다
- 안 적으면 화면에 아예 안 나온다. 지금 이야기를 쓸 때는 적지 않는다
- 앞으로 쓰는 글에 이걸 안 달면 전부 쓴 날짜만 달고 쌓여서, 나중에 또 발굴해야 한다

**`strand` — 이 항목이 걸리는 갈래(관심사).** 섹션은 **매체**로 나뉘지만 생각은
**관심사**로 묶인다. 음식에 대한 책과 음식 앱이 다른 섹션에 있어도 같은 갈래로 이어진다.

```yaml
strand: 먹는 일, 재는 일        # 쉼표로 여럿
```

- 값은 **「나」의 "지금 관심이 벌어져 있는 자리들"에 적힌 아홉 개 이름을 그대로** 쓴다.
  목록은 `app/threads/page.tsx`의 `STRANDS`에 있다. **새 이름을 지어내지 않는다**
- **억지로 다 채우지 않는다.** 아홉 갈래 어디에도 안 맞으면 안 붙인다.
  비어 있는 갈래가 있어도 그대로 둔다
- 목록(`list: true`)에는 안 붙는다. `getByStrand()`가 낱개 글만 훑는다

**`then: true` — 그때 적어둔 것을 옮긴 글.** 화면에 *"2021년에 적어둔 것을 옮겼습니다"*
가 붙는다(연도는 `date`에서). **발굴로 올라오는 글에는 반드시 붙인다.**

이건 **시점을 밝히는 용도이지 "지금과 다를 수 있다"는 단서를 다는 용도가 아니다.**
옛 글 대부분은 지금과 이어져 있다. 이질적인 것은 올리기 전에 걸러야지, 올려놓고 각주로
면책할 일이 아니다.

- 날짜는 반드시 `YYYY-MM-DD`. YAML이 Date 객체로 바꿔버리므로 `lib/content.ts`의 `toISODate()`를 거쳐야 한다.
- `work`, `writing`, `reading`의 파일명은 URL이 되므로 **영문 소문자·숫자·붙임표만** 쓴다. (`me`는 상세 페이지가 없어 자유)

### 목록 — 앞머리에 `list: true`

본문 없이 제목만 쌓이는 목록(읽은 책 133권 같은 것)은 낱개 글이 아니라 **파일 하나에 표**로
넣는다. 수백 권을 개별 파일로 만들면 빈 상세 페이지가 수백 개 생긴다.

앞머리에 `list: true` 한 줄만 넣으면 목록이 된다. **목록을 늘릴 때도 코드를 고치지 않는다.**

```markdown
---
title: 대학원에서 읽은 교재
date: 2026-09-10
summary: 수업에서 쓴 책들
list: true
---

(안내문)

| 제목 | 지은이 | 연도 |
| --- | --- | --- |
| 책 이름 | 지은이 | 2024 |
```

- 한 줄에 하나. 둘째·셋째 칸은 비워도 된다. 넷째 칸부터는 무시되므로 열을 늘려도 안 깨진다
- **셋째 칸은 목록마다 뜻이 달라도 된다.** 군대 목록은 연도로, 교재 목록은 과목으로 쓴다.
  화면에는 오른쪽에 작게 붙는다
- 제목에 세로줄(`|`)은 쓰지 않는다
- **`_`로 시작하는 파일은 항목이 아니다.** `_notice.md`처럼 여러 화면이 함께 쓰는 조각을
  둔다. 낱개 글로 잡히지 않고 주소도 생기지 않는다
- 파서(`getList()`)는 **어떤 입력에도 예외를 던지지 않는다.** 줄이 깨지면 그 줄만 빠진다
- **목록을 낱개 글보다 먼저 둔다.** 2026-09-11에 「읽은 것」에서 낱개 글 여섯 편이 위에
  큰 제목으로 떠서 *"이것들이 대표작처럼 보인다"*는 지적을 받았다. 실제로는 198권 중
  글이 붙은 것이 여섯일 뿐 고른 것이 아니다. **분량이 많은 쪽을 먼저 보여주고**, 낱개 글
  묶음 위에는 그것이 선별이 아님을 한 줄로 밝힌다 (`.blockhead`)
- **`unread: true`** — 이 목록의 책은 아직 안 읽은 것이다(「읽고 싶은 책들」).
  "읽은 책이 몇 권"을 셀 때 빠진다. 안 붙이면 읽은 것으로 센다
- **`_about.md`** — 섹션 화면 끝에 붙는 설명. `getNotice(dir, "about")`으로 부른다.
  `_notice.md`와 같은 `_` 규칙이라 낱개 글로 잡히지 않는다
- 같은 제목으로 낱개 글을 쓰면 목록 행이 **자동으로** 그 글로 이어진다
- 낱개 글과 목록이 `[slug]` 경로를 함께 쓴다. 화면만 다르게 그린다

## 어겨서는 안 되는 규칙 — 공개 금지 자료

이 사이트는 **누구나 볼 수 있고 검색에도 잡힌다.** 바탕화면(`~/Desktop`)과 그 하위 폴더
(`대학원`, `디지털 치료제`, `카이스트 명상과학연구소 프로그램 개발`, `liberal arts` 등)를 참고 자료로
읽는 것은 괜찮지만, 아래는 **어떤 형태로도 사이트에 옮기지 않는다.** 요약·인용·익명화한 형태도 안 된다.

**절대 올리지 않는다**
- 실제 내담자·피검자 자료 — WAIS·로르샤흐·SNSB 프로파일, 결과지, 사례 기록. 이름이 일부만 가려진 것(`민O홍`)도 포함
- 법적·사법 문서 — 판결문(존속살해 등), 정신감정 자료, IRB 서류, 입양평가 기록
- 본인 신원·행정 서류 — 혼인관계증명서, 병적증명서, 건강보험 자격득실확인서, 등록금 고지서, 장학금 신청서, 수험 서류
- 제3자의 미공개 자료 — 랩미팅 발표자료, 수업 배포자료 원문, 미출판 원고, 남의 발표 PPT
- 저작권 있는 교재·논문 PDF의 본문
- **음식중독 프로젝트(`~/Desktop/my program/Is Food Addictive`)의 자가점검 문항** —
  검증된 척도(YFAS 등)는 라이선스가 확인되지 않았고, **자체 개발 문항도 올리지 않는다.**
  임상 전문가 검토 전에는 배포하지 않기로 한 본인 결정과 어긋난다.
  기획서·분석 문서의 **판단과 설계 원칙은 올려도 된다** (본인이 쓴 것이므로)
- **SNS 화면 캡쳐 원본** — 계정명, 좋아요 수, 사적인 사진이 함께 담긴다. 참고 자료로 읽되
  캡쳐 파일 자체는 저장소에 넣지 않는다 (`.gitignore` 참조). 옮길 내용은 손으로 골라 쓴다

**1인칭 문장은 근거가 있어야 한다 — 가장 중요한 규칙**

이 사이트의 "나"·"읽은 것"·"글"에 들어가는 1인칭 문장은 **조은찬 본인이 실제로 한 말**에
근거해야 한다. 근거는 `_원본자료/검증된-인용-색인.md`에 모여 있다.

- 근거가 없으면 **쓰지 않는다.** 분량이 줄어도 그대로 둔다. 그럴듯하다는 이유로 지어내지 않는다
- **본인이 예전에 쓴 문구를 그대로 옮기지 않는다.** 인스타 캡션, 옛 메시지, 옛 글 전부 해당한다.
  본인이 옛 글을 다시 보는 걸 부담스러워한다 — 의미만 살려 **문장은 새로 쓴다.**
  지어내지 않는 것과 그대로 옮기는 것은 다른 문제다. 근거는 원문에 두되 표현은 바꾼다
- 자리표시자로 넣은 문장을 나중에 근거처럼 다루지 않는다 (2026-09-09에 실제로 벌어진 일이다)
- 과거에 읽은 책을 현재 관심사의 **원인**으로 서술하지 않는다.
  **다만 이 규칙은 관련 자체를 부정하라는 뜻이 아니다.** 본인의 정정 (2026-09-11):
  *"인과로 확정은 아니라는 뜻, **관련성이 있을 순 있겠지.** 다만 **지나친 환원주의에
  빠지지 말라**는 뜻이었어."*
  → 막는 것은 **환원**이다. *"이 책들이 지금의 관점을 만들었다"* 로 줄이면 그 사이에
  있던 것이 전부 지워진다. **관련이 있다고 적는 것은 되고, 그것으로 설명된다고 적는 것은
  안 된다.** *"모르겠다"* 로 닫아버리는 것도 과했다
- 질문 하나를 인생 전체의 태도 선언으로 확장하지 않는다
- **문장뿐 아니라 배치로도 과장하지 않는다.** 채팅에서 나온 질문 하나에 제목을 달아 독립
  항목으로 세우면, 그 배치 자체가 "이게 내 대표 주제다"라는 주장이 된다.
  2026-09-10에 실제로 벌어져서 다섯 항목을 하나로 합쳤다
- 날짜·기간·횟수는 본인이 준 값이나 캡션에서 읽히는 값만 쓴다. 근사치를 채워 넣지 않는다
- **옛 글을 옮길 때는 본문을 과거형으로 쓰고 `then: true`를 붙인다.** 구체적 인용과 자료가
  없는 한, 그때의 관찰을 현재 판단처럼 쓰지 않는다. 다만 **과거와 현재를 갈라놓는 것이
  목적이 아니다** — 대부분은 이어져 있고, 이질적인 것만 본인이 검토해 걸러낸다
- **남의 문장을 옮길 때는 인용임과 출처를 밝힌다.** 출처를 모르면 모른다고 적는다.
  밝히지 않으면 본인 주장으로 읽힌다
- **인용을 옮겨 적은 것도 근거다.** 남의 문장이라는 이유로 자료에서 빼지 않는다. 본인의 정정:
  *"인용을 옮겨적었다는 건 마음에 울림을 주는 무언가를, 그 문장을 통해 발견했다는 거로
  이해해 나는."* 무엇을 옮겨 적을지 고르는 것도 고르는 일이다. **인용임을 밝히고 세되,
  빼지는 않는다** (2026-09-10에 「권력의 심리학」을 이 이유로 잘못 뺐다)
- **주제의 일관성을 위해 자료를 골라내지 않는다.** 본인 지시: *"주제나 일관성에 따라 특정
  책을 배제할 필요는 없어."* 결이 안 맞는 것을 빼면 주장만 남고 근거가 사라진다
- **본인이 던진 질문에 Claude가 답을 채우지 않는다.** *"왜 그랬을까"* 같은 질문을 받으면
  **근거를 모아 보여주는 데까지**가 Claude 몫이다. 무엇과 무엇을 잇는지는 본인이 정한다.
  **문장으로 답하지 않는 것만으로는 부족하다 — 근거를 나란히 놓는 배치도 답이다.**
  질문은 사이트에 올려도 되고, 답이 빈 채로 두는 것이 정직하다

**종교·정치 — 어디까지 되는가**

이전 규칙("종교·정치로 읽힐 여지를 만들지 않는다")은 너무 넓게 잠근 것이었다. 본인의 정정:

> 종교적, 정치적 입장으로 읽힐 여지가 0%도 있어선 안되는 건 아니야. 내 생각이 투영된 거기
> 때문에 읽힐 수 있는 게 당연하다고 생각해. (…) 순수한 의미의 종교, 순수한 의미의 정치에
> 대해서는 관심이 많아.

- **하지 않는 것** — 특정 정당·정파·교단을 편들거나 그 논쟁에 참전하는 것처럼 보이는 문장.
  현실 정치·현실 종교의 반목 구도 위에 사이트를 세워두지 않는다
- **해도 되는 것** — 주제로서의 종교·정치. 본인 생각을 적었더니 어떤 입장으로 읽히는 것까지
  피하려 하지 않는다. 생각을 적으면 읽히는 것이 당연하다
- 판단이 서지 않으면 빼지 말고 **먼저 묻는다.** 본인 발언을 Claude가 걸러내는 것도 월권이다

**연락처 — 결론 남**

`app/layout.tsx`의 `a5897@icloud.com`을 **그대로 둔다.** iCloud 별칭도 만들어봤으나
무작위 문자열이라 개인 사이트 하단에 놓으면 임시 주소처럼 보인다. 읽기 쉬운 쪽을 택했다.
수확 위험은 iCloud 스팸 필터로 감당한다. 다시 논의하기 전까지 바꾸지 말 것.

**올려도 되는 것**
- 수업명, 스터디 주제, 관심 분야 — 즉 "관심사의 모양"
- 공개된 논문·책의 제목과 지은이
- 조은찬 본인이 직접 쓴 생각·정리·글
- 본인이 만든 것(앱, 프로그램)에 대한 설명

판단이 서지 않으면 **올리지 않고 먼저 묻는다.** 이 사이트에서 되돌리기는 늦다 — 한 번 배포되면
검색엔진과 아카이브에 남는다.

## 명령어

```bash
npm run dev     # 로컬 확인 (http://localhost:3000)
npm run studio  # 항목 만들기 화면 (http://localhost:4321). 이 컴퓨터에서만 열린다
npm run build   # 정적 내보내기 → out/
git push        # GitHub Pages(약 60초) + Vercel(약 10초) 자동 배포
```

## 주의

- `out/`, `.vercel`, `.env*`는 커밋하지 않는다 (`.gitignore`에 있음)
- GitHub Pages는 Actions 빌드 방식(`build_type: workflow`)이어야 한다. 저장소 브랜치 방식으로
  되돌아가면 `_next/` 폴더를 Jekyll이 무시해서 CSS가 전부 깨진다.

## `npm run studio`

`studio/server.mjs` — 의존성 없는 독립 스크립트. **Next 안에 두지 않는다.**
이 사이트는 `output: "export"`라 서버가 없어서, 파일을 쓰는 라우트를 Next에 넣으면
정적 내보내기가 깨진다. `127.0.0.1`에만 열어 다른 기기에서는 접속되지 않는다.

- 저장과 올리기는 **반드시 두 단계**다. 저장 뒤 무엇이 올라가는지 다시 보여주고 그다음에 버튼
- **`content/` 밖의 변경이 있으면 올리기를 막는다.** 코드 수정이 딸려 올라가는 걸 방지한다.
  그럴 때는 터미널에서 직접 올린다
- 기존 파일은 덮어쓰지 않는다. 제목·날짜·주소 형식이 틀리면 파일을 아예 만들지 않는다
- `git push` 실패는 화면에 그대로 보여준다
