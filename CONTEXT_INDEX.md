# Context Index

이 프로젝트의 장기 맥락은 `docs/context/`에 나뉘어 있다.
**전부 읽지 않는다. 지금 하는 일에 걸리는 것만 읽는다.**

무엇을 하려는지 모르겠으면 `site-purpose.md` 하나만 읽고 시작한다.

## 사이트 방향

- **`docs/context/site-purpose.md`**
  이 자리가 무엇을 위한 것인가, **어떻게 조직하는가(편년체가 아니라 기전체)**,
  표현하려는 것, 섹션 두께 원칙, 종교·정치 정책.
  → 「나」·소개·전체 방향을 손볼 때. 어떤 글을 올릴지 결이 안 잡힐 때.
  → **새 화면·기능·시각화를 제안하기 전에는 반드시.**

## 콘텐츠

- **`docs/context/content-structure.md`**
  다섯 칸, 항목 추가 두 가지 방법, front matter 전체 규격(`about`·`strand`·`then`·
  `read`·`list`·`unread`), 목록 표 규격, `_` 파일, 배치 순서 규칙.
  → 항목을 추가·수정할 때. 목록을 늘릴 때. **이것만 읽으면 코드를 안 봐도 된다.**

- **`docs/context/reading.md`**
  같은 책을 두 때에 읽은 기록이라는 축, 책 글의 세 층, `read:`의 뜻, 재발견이라는 목적,
  비공개 책노트와의 경계.
  → 「읽은 것」의 책 글을 쓰거나 고칠 때.

## 화면

- **`docs/context/design.md`**
  본문 폭·여백·타이포를 건드리지 않는 이유, 길을 내는 규칙, SVG 규칙, 연락처 확정 결정.
  → 레이아웃·항해·지도·그림을 손볼 때.

## 도구

- **`docs/context/build-and-deploy.md`**
  명령어, 커밋하면 안 되는 것, GitHub Pages 빌드 방식, `npm run studio`의 설계 이유.
  → 빌드·배포·studio를 다룰 때.

## 원본 자료

- **`docs/context/source-material.md`**
  gitignore된 `_원본자료/`에 무엇이 어디 있는지, `[사실]`·`[추측]`·`[?]`·`[후보]` 표기.
  → **1인칭 문장을 쓸 때. 옛 기록의 근거를 찾을 때.**

## 지금 하던 일

- **`HANDOFF.md`** (gitignore)
  최근 변경, 미완료 작업, 확인 필요한 것. **장기 맥락이 아니라 현재 상태다.**
  → 세션을 시작할 때 한 번. 세션을 끝낼 때 갱신.

---

**항상 걸리는 규칙(공개 금지 자료, 1인칭 근거)은 이 색인이 아니라 `CLAUDE.md`에 있다.**
지연 로딩에 맡기지 않는다.
